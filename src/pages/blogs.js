import React from 'react';

const Blogs = () => {
return (
	<h1>React is one of the most wide-spread UI libraries for web app development. 
        It is simple, efficient, and has an extensive structure of components and support libraries. 
        It is precisely this complexity that makes learning React particularly daunting to learn.</h1>
);
};

export default Blogs;
